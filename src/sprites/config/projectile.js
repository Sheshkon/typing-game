import {Animation} from '@/types/animation.js';

export const projectileSpritesConfig = {
    [Animation.IDLE]: {
        frames: 2,
        width: 64,
        height: 32,
        speed: 50,
        loop: true,
        scale: 2,
    }
};
